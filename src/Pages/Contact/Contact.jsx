import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Contact = () => {
    useTitle('Contact')
    return (
        <div>
            <p>djf</p>
        </div>
    );
};

export default Contact;